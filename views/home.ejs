<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <!-- <link rel="stylesheet" href="<%= `style.css` %>" /> -->
    <link rel="stylesheet" href="/css/bootstrap.min.css">
  </head>
  <body>
    <div style="display: flex; flex-direction:column; text-align:center; background:linear-gradient(#FCFFE0, #FFFBDA); min-height: 100vh">
      <div style="display: flex; justify-content: space-around;" class="mt-2">
        <div>
          <h2>Welcome to Blog-website</h2><br>
          <form action="/article" method="GET" >
            <input type="submit" value="Create new article"  class="btn btn-primary" />
          </form>
        </div>

        <div>
          <h3 style="display: inline;">welcome, <%= userName %></h3> &nbsp;
          <form action="/logout" method="post" class="mt-3">
            <input type="submit" value="logout" class="btn btn-danger" />
          </form>
        <form action="/myblogs" method="get">
          <input type="submit" value="My Blogs" class="btn btn-success m-3">
        </form>
        </div>
      </div>
      <div style="display: flex; flex-direction:column; align-items:center;">
        <% if (allArticles === "[]") { %>
            <div style="margin-top: 20%;">
              <h1>NO BLOGS FOUND 😔</h1>
            </div>
        <% } else { %>
            <% allArticles.forEach(article => { %>
        <div style="width: 80%; margin: 2%;" class="border border-primary rounded">
          <h3><%= article.title %></h3>
          <img
            src="<%= `thumbnails/${article.thumbnail}` %>"
            alt="pic"
            class="rounded img-thumbnail w-25"
          />
          <h3>By:- <%= article.writer %></h3>
          <p><%= article.body %></p>
        </div>
        <% }) %>
        <% } %>
         
      </div>
    </div>
  </body>
</html>

<!-- <img src="../storage/thumbnails/article.thumbnail" alt="" /> -->
